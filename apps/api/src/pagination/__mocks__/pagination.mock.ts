import type { PaginationMeta } from '@repo/schemas'

export const mockMetaPagination: PaginationMeta = {
	itemsPerPage: 10,
	currentPage: 1,
	totalItems: 0,
	totalPages: 0,
	hasNextPage: false,
	hasPreviousPage: false
}
